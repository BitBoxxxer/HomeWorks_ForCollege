<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Height="800" Width="750"
        WindowStartupLocation="CenterScreen"
        Background="Transparent">
    
    <Window.Resources>
        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FF6B6B" Offset="0"/>
                        <GradientStop Color="#FF8E53" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="25"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="0.85" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 To="1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
    </Window.Resources>

    <Border Background="White"
            CornerRadius="20"
            Margin="10"
            BorderBrush="#DDD"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" Opacity="0.2" ShadowDepth="3"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,15,0">
                <Button Content="🗕" Width="30" Height="30" Background="Transparent" BorderThickness="0" Click="MinimizeWindow" Cursor="Hand" FontSize="16"/>
                <Button Content="🗙" Width="30" Height="30" Background="Transparent" BorderThickness="0" Click="CloseWindow" Cursor="Hand" FontSize="16" Margin="5,0,0,0"/>
            </StackPanel> -->
            <TextBlock Grid.Row="0"
                       Text="МОНИТОРИНГ ТЕМПЕРАТУРЫ"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#FF6B6B"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            <Button Grid.Row="1"
                    x:Name="StartStopButton"
                    Content="СТАРТ"
                    Style="{StaticResource AccentButton}"
                    Click="OnStartStopClick"
                    HorizontalAlignment="Center"
                    Margin="0,10"/>
            <Border Grid.Row="2"
                    Background="#F8F9FA"
                    CornerRadius="12"
                    Padding="15"
                    Margin="0,20,0,10">
                <StackPanel>
                    <TextBlock x:Name="StatusText"
                               Text="Остановлен"
                               FontSize="14"
                               Foreground="#6C757D"
                               TextAlignment="Center"/>
                    <TextBlock x:Name="LastTempText"
                               Text="Последняя: --"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#2C3E50"
                               TextAlignment="Center"
                               Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

            <TextBlock Grid.Row="3"
                       Text="Каждые 5 сек. отправляется случайная температура 10–40 °C"
                       FontSize="11"
                       Foreground="#ADB5BD"
                       HorizontalAlignment="Center"
                       Margin="0,15,0,0"/>
        </Grid>
    </Border>
</Window>